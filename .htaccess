<IfModule mod_rewrite.c>
    RewriteEngine On

    # Nếu không phải folder/file thực tế
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Chuyển tất cả request vào public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
